import React from 'react';
import LoginPage from './LoginPage';

interface Props {
    // Define props here
}

const LoginRouteComponent: React.FC<Props> = (props) => {
    // Component logic here

    return (
        <>
        <LoginPage />
        </>
    );
};

export default LoginRouteComponent;
